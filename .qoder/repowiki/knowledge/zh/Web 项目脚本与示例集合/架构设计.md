- 采用扁平化目录结构组织不同功能的子模块，包括 `ci&cd`（持续集成/部署脚本）、`handwritten-code`（手写代码示例）、`practice`（练习项目）及 `test-space`（组件库测试）。
- 通过 `build.sh` 入口脚本触发特定子模块（如 `env-prepare`）的构建流程，依赖 `.nvmrc` 锁定 Node.js v18.20.2 运行时环境。
- 利用 `.gitignore` 和 `.prettierignore` 统一管理多语言生态（Node.js, Yarn, Pnpm 等）的忽略规则，确保开发环境的一致性。